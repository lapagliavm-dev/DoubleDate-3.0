import React from 'react';
import ReactDOM from 'react-dom/client';
import DoubleDate from '../DoubleDate.jsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <DoubleDate />
  </React.StrictMode>
);